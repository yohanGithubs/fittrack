/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    typescript: {
      // Optionally ignore build errors if you encounter any
      ignoreBuildErrors: true
    }
  }
  
  module.exports = nextConfig